# 2026-08-29 status

live: https://ident.greg.technology (disco project `ident`, `disco deploy:output --project ident --disco my-paas-prod`)

## done
- vite vanilla-ts, dockerfile + disco.json (generator service)
- three columns: rail (mark + move) / stage with destination tabs (site header, 404, readme, terminal)
- webgl scan-deflection engine (src/gl/warp.ts) ported from the 15.scanimate-spike warp pass, persistence via ping-pong
- moves = deflection chords driven by a damped spring envelope; enter (on load) + react (on hover)
- ascii = alpha coverage only, colors never leak, bg is composited per destination
- readme context = real github screenshot, slot pinned at (80,525)-(1623,1058) of 1676x1372
- terminal: no hover, enter move loops every 3s
- site header: iframe of a real url (sandbox allow-scripts), fake chrome hides once loaded

## explained (was: faint rectangular ghost)
- persistence stored prev*decay in 8-bit: 1/255 * 0.6 rounds back to 1/255, so any trace never fully decayed. fixed with a -1.5/255 bias in the warp shader. after settling only antialiased text edges have partial alpha.

## next
- exports are stubs: .sh (frames + sleep loop), 404.html (self-contained ascii loop), mp4/webm (deterministic fixed-dt render), js embed
- screen pass from the spike (glow / scanlines / barrel / colorizer) not ported
- tab thumbnails in ascii mode are tiny/faint
- share urls / db: not started
